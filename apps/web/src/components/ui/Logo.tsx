export function Logo({ width, height }: { width: number; height: number }) {
  return (
    <img
      src="/images/branding/logo.svg"
      alt=""
      width={width}
      height={height}
    />
  );
}
